import { FC } from 'react';
import { Title, Container } from "@mantine/core";

const Landing: FC = () => {
	return (
    <Container>
      <Title order={1}>Welcome to the SpaceX Dashboard</Title>
    </Container>
  );
};

export default Landing

